const os = require("os");

// 1. to get the os platform & user info 
// return the platform of current operating system
console.log("platform: " + os.platform());
// return the user info
console.log(os.userInfo());

